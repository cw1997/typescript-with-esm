{
  "name": "typescript-with-esm",
  "version": "1.0.0",
  "description": "",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "start": "pnpm --filter @cw1997/test-app run start",
    "build:webpack":  "turbo run build:webpack",
    "build:rollup": "turbo run build:rollup",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
