{
  "name": "typescript-with-esm",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build:api": "pnpm --filter @typescript-with-esm/api run build:tsc",
    "start:api": "pnpm --filter @typescript-with-esm/api run start:prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=18"
  }
}
